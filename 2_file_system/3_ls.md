# Jelenlegi könyvtár listázása
```bash
ls
```
Ha szeretnénk részletes információkat is kilistázni:
```bash
ls -l
```

Rejtett elemek, illetve egyéb referenciák:
```bash
ls -a
# csak rejtett elemek
ls -A
```
